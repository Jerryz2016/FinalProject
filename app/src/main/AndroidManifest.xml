<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jieli.finalproject">

    <supports-screens android:requiresSmallestWidthDp="600" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".HouseSettingStartActivity">

        </activity>
        <activity android:name=".HouseSettingStartActivity" />
        <activity android:name=".CarSettings" />

        <activity android:name="Temperature" />
        <activity android:name=".LivingRoomActivity" />
        <activity android:name=".KitchenStartActivity" />
        <activity android:name=".Tv" />

        <activity android:name=".LivingDatabaseActivity"></activity>

        <activity android:name=".CarTemperature" />
        <activity android:name=".LivingRoomActivity"></activity>
        <activity android:name=".KitchenStartActivity" />
        <activity android:name=".CarLights"></activity>
        <activity
            android:name=".CarRadio"
            android:windowSoftInputMode="adjustPan"></activity>
        <activity android:name=".CarRadioChange"></activity>

    </application>

</manifest>
